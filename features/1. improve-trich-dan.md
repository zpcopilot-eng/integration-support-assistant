1. Sửa trích dẫn nguồn:

Sửa prompt: yêu cầu copy chính xác chuỗi sau [Nguồn: ...] từ kết quả search_docs, không tự diễn giải.
Hoặc thêm post-processing: validate các path trong câu trả lời có khớp file thật trong docs/, nếu không khớp thì sửa/loại bỏ.

2. Chunk theo heading (##) khi index: index_docs.py tách mỗi file thành các section nhỏ, gắn [Nguồn: file.md#heading] → cải thiện độ chính xác retrieval và trích dẫn.

3. Cơ chế log "doc gap": khi agent trả lời "không tìm thấy thông tin", tự động remember câu hỏi đó vào một namespace riêng (ví dụ /strategies/doc-gaps/...) hoặc gửi vào kênh Telegram/log để team docs review định kỳ.

4. Tự động re-index: thêm GitHub Action / pre-deploy hook chạy scripts/index_docs.py khi có thay đổi trong docs/.

5. Bộ eval câu hỏi mẫu: tạo file tests/golden_questions.md (câu hỏi + nguồn mong đợi) để chạy regression nhanh sau khi sửa prompt/docs.